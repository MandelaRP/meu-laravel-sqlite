# ============================================
# Configuração de Ambiente para Render.com
# ============================================
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env no Render
# 2. Gere a APP_KEY: php artisan key:generate
# 3. Configure as variáveis específicas do seu projeto
# 4. Ajuste APP_URL com a URL do seu app no Render
#
# ============================================

# Aplicação
APP_NAME=LuckPay
APP_ENV=production
APP_KEY=base64:GENERATE_KEY_USING_ARTISAN
APP_DEBUG=false
APP_URL=https://meu-laravel-sqlite.onrender.com
APP_TIMEZONE=America/Sao_Paulo

# Banco de Dados - SQLite (Render)
# O caminho /opt/render/project/src/ é o diretório raiz do projeto no Render
DB_CONNECTION=sqlite
DB_DATABASE=/opt/render/project/src/database/database.sqlite

# Banco de Dados - MySQL (Alternativa)
# Se preferir usar MySQL no Render, descomente e configure:
# DB_CONNECTION=mysql
# DB_HOST=seu-host-mysql.render.com
# DB_PORT=3306
# DB_DATABASE=nome_do_banco
# DB_USERNAME=usuario
# DB_PASSWORD=senha

# Logs
LOG_CHANNEL=stack
LOG_LEVEL=error
LOG_DEPRECATIONS_CHANNEL=null

# Cache
CACHE_DRIVER=file
CACHE_PREFIX=

# Sessões
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# Fila (Queue)
QUEUE_CONNECTION=database
QUEUE_QUEUE=default

# Broadcasting
BROADCAST_DRIVER=log

# Email
MAIL_MAILER=smtp
MAIL_HOST=smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# AWS (se usar S3 para storage)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=us-east-1
# AWS_BUCKET=
# AWS_USE_PATH_STYLE_ENDPOINT=false

# Redis (se usar Redis no Render)
# REDIS_HOST=127.0.0.1
# REDIS_PASSWORD=null
# REDIS_PORT=6379

# ============================================
# Variáveis Específicas do LuckPay
# ============================================

# Configurações de Pagamento (ajustar conforme necessário)
# LIBERPAY_API_URL=
# LIBERPAY_API_KEY=
# FULLPIX_API_URL=
# FULLPIX_API_KEY=

# ============================================
# Configurações de Segurança
# ============================================

# CSRF
SESSION_SECURE_COOKIE=false

# ============================================
# Notas Importantes para Render
# ============================================
#
# 1. SQLite no Render:
#    - O arquivo database.sqlite deve existir em database/
#    - O diretório database/ deve ter permissões de escrita
#    - Execute: touch database/database.sqlite antes do deploy
#
# 2. Storage:
#    - O diretório storage/ deve ter permissões de escrita
#    - Execute: php artisan storage:link após o deploy
#
# 3. Cache:
#    - Execute: php artisan config:cache após configurar .env
#    - Execute: php artisan route:cache
#    - Execute: php artisan view:cache
#
# 4. Migrações:
#    - Execute: php artisan migrate --force no build
#
# 5. Build Command no Render:
#    composer install --optimize-autoloader --no-dev
#    npm install
#    npm run build
#    php artisan key:generate --force
#    php artisan migrate --force
#    php artisan storage:link
#    php artisan config:cache
#    php artisan route:cache
#    php artisan view:cache
#
# 6. Start Command no Render:
#    php artisan serve --host=0.0.0.0 --port=${PORT}
#
# ============================================

